/** Fixed hero height — use on slider track so slides can stack absolutely. */
export const HERO_SLIDE_HEIGHT = "h-[540px] sm:h-[620px] md:h-[680px]";

/** Aligns hero copy with navbar / main content (1400px + site padding). */
export const HERO_INNER_CONTAINER = "mx-auto w-full max-w-content px-4";
