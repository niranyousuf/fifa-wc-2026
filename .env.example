# Primary key (required if ZAFRONIX_API_KEYS is not set)
ZAFRONIX_API_KEY=your_zafronix_api_key_here

# Optional: extra keys — used in order when a key hits 429 rate limit
# ZAFRONIX_API_KEYS=your_second_key,your_third_key

WC_YEAR=2026
# Used for Open Graph / social share previews (set to your production URL on Vercel)
NEXT_PUBLIC_APP_URL=https://fifa-wc-2026-alpha.vercel.app

# Visitor counter (production on Vercel — add Upstash Redis from Marketplace, connect to project)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# Legacy Vercel KV names still work if your integration sets these instead:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
